#Ici nous allons tester sur 2 vues si notre code fonctionne. Pour cela, il nous faut la matrice de rotation de la caméra relative à la deuxième vue. 

import json
import numpy as np
import cv2


with open("data/absolute_transforms.json", "r") as f:
    camera_data = json.load(f)["0"]

print(camera_data)

ref_view_id = "26"
ref_R_vec = np.array(camera_data[ref_view_id][0])
ref_T = np.array(camera_data[ref_view_id][1])

ref_R, _ = cv2.Rodrigues(ref_R_vec)

print(ref_R_vec)
